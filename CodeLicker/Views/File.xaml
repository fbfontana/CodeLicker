<UserControl x:Class="CodeLicker.Views.File"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:p="clr-namespace:CodeLicker.Properties"
      xmlns:local="clr-namespace:CodeLicker.Views"
      xmlns:ViewModels="clr-namespace:CodeLicker.ViewModels"
      mc:Ignorable="d" 
      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
      >

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">

        <Grid Name="TabContent" Background="White" MinWidth="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="15" />
                <RowDefinition Height="50" />
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="15" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="150*"/>
                <ColumnDefinition Width="600*"/>
                <ColumnDefinition Width="150*"/>
                <ColumnDefinition Width="15"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" 
                     Text="{x:Static p:Resources.CodeAnalysisReport}" 
                     HorizontalContentAlignment="Center"
                     BorderThickness="0"
                     Style="{DynamicResource LatoLight}" Foreground="#CF000F"
                     FontSize="36"/>
            <Separator Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Background="#CF000F"/>
            <Label Content="Path" Foreground="#CF000F" 
               HorizontalAlignment="Stretch" VerticalAlignment="Top"
               Grid.Row="3" Grid.Column="1" HorizontalContentAlignment="Right" />
            <TextBox Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"
               HorizontalAlignment="Stretch" VerticalAlignment="Top"
               Grid.Row="3" Grid.Column="2" Height="20"  TextWrapping="Wrap" Margin="5,4,-5,0"/>

        </Grid>

    </ScrollViewer>
</UserControl>
